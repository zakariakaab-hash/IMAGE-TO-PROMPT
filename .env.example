# ==============================================================================
# Cloudflare Workers Environment Configuration
# ==============================================================================
# The application uses Cloudflare Workers with native `env.AI` bindings.
# Deployment is managed automatically via Cloudflare Workers Builds GitHub integration.
#
# No runtime API tokens, account IDs, or secrets are required in the application.
# ==============================================================================
